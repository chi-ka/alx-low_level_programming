#include <stdio.h>
#include "main.h"

/**
 * main - multiply two integers.
 *
 * @a: one of the integers to multiply
 *
 * @b: one of the integers to multiply
 * 
 * Return: 1 if uppercase and 0 otherwise.
 */

int mul(int a, int b)
{
        int c;
        
        c = a * b;
        
        return (c);
}
